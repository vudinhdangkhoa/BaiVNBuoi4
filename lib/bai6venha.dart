import 'package:flutter/material.dart';

class Bai6venha extends StatelessWidget {
  const Bai6venha({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
